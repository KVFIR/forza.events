# Dependencies
node_modules

# Build
dist
dist-ssr
*.local

# Logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
*.sw?
.cursor/

# Env (keep example tracked)
.env
.env.*.local

# TypeScript
*.tsbuildinfo

# Vite
.vite

# Supabase CLI local state
supabase/.temp/

# Deno (local cache from Edge deploy / tooling)
deno.lock

# Python
__pycache__/

# Graphify (generated AST graph)
graphify-out/

# Impeccable (keep DESIGN.md, PRODUCT.md, .impeccable/config.json, briefs, mocks)
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/hook.pending.json
.impeccable/questions/
.impeccable/critique/
.impeccable/live/*
!.impeccable/live/config.json
