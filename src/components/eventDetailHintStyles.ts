/** Shared Event Detail callout frame — quiet outline, no heavy fill. */
export const eventDetailHintClass = 'border-white/20 bg-white/[0.03] py-2.5 text-sm';

/** Ranked explainer — amber outline aligned with RankedBadge. */
export const eventDetailStatusHintClass = 'py-2.5 text-sm';
